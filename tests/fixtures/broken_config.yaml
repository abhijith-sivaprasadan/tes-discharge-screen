# Deliberately broken: process.annual_peak_load_mw is missing, and an
# unexpected key `notes` was added. Used by test_config.py to prove the
# loader fails loudly and names the contract violation, per Phase 0's exit
# criterion.

case_name: broken_example

process:
  delivery_temperature_c: 300.0
  medium: steam
  profile_shape: flat
  profile_path: data/profiles/packed_bed_300c_flat.csv
  notes: this key does not belong here

storage:
  technology: packed_bed
  energy_capacity_mwh: null
  charge_power_max_mw: null
  discharge_power_max_mw: null
  eta_charge: 0.95
  eta_discharge: 0.95
  standing_loss_fraction_per_hour: 0.002
  temperature_max_c: 400.0
  temperature_min_c: 320.0
  soc_init_fraction: 0.5
  soc_final_min_fraction: 0.5
  discharge_limit_mode: constant

supply:
  electric_heater:
    efficiency: 0.99
    capacity_mw: 15.0
  backup_boiler:
    fuel: natural_gas
    fuel_cost_eur_per_mwh: 40.0
    emission_factor_kg_co2_per_mwh: 202.0
    capacity_mw: 12.0
  electricity_price_source: entso_e

economics:
  currency: EUR
  discount_rate: 0.06
  storage_lifetime_years: 25.0
  storage_capex_eur_per_mwh: 30000.0
  storage_capex_eur_per_mw: 60000.0
  carbon_price_eur_per_tco2: 80.0

optimization:
  solver: highs
  horizon_hours: 8760
  time_limit_seconds: 300.0
  mip_gap: 0.01
